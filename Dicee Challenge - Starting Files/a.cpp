#include <bits/stdc++.h>  
using namespace std;
 
typedef long long ll;
typedef long double ld;
const ll MOD = 1e9+7;
#define all(x) (x).begin(), (x).end()
 
void solve(){
    //code
    int n;
    cin >> n;
    vector<int>a(n);
    for(auto &x: a) cin >> x;
    // vector<int>a(n);
    for(auto &x: a) cin >> x;
}
 
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    ll t;
    cin >> t;
    while(t--) solve();
    return 0;
}